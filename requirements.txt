# Core watcher
watchdog>=4.0.0

# AI planning (optional — falls back to local planner if key not set)
openai>=1.0.0

# Gmail watcher (Silver Tier)
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
